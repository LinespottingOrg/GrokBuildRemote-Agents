# binaries / build
/dist/
*.exe
.gbr-agent*

# local Go toolchain (portable)
.goroot/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# secrets
.env
.env.*
api-passwords.md
*.pem
*.key

# relay: real Cloudflare config carries account_id + KV namespace id.
# Commit wrangler.example.toml instead.
relay/wrangler.toml
relay/.wrangler/
relay/node_modules/

# QA output — agent logs, trace JSONL, pairing codes, device.json backups
qa/logs/
